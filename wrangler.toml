# 与 Cloudflare Dashboard 里的 Workers 项目名保持一致
# 账号级 workers.dev 子域已改为 kk；此处 worker 名也为 kk → 线上地址 https://kk.kk.workers.dev
name = "kk"
compatibility_date = "2026-08-11"

# Workers 静态资源：直接把 dist/ 目录作为站点发布
[assets]
directory = "./dist"
