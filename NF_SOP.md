# NF 阅读规划 · SOP（两种模式）

用户定义的两种用法。目标都是产出「内容大纲 + 画面指令」给后续 ImageGen/HTML 用，但触发方式不同。

---

## 模式 1 · 自动规划（拿到新级别）

**输入**：一个级别（如 F）
**我做**：
1. 取该级别全部 NF 书。
2. 按「主题相似度」聚类成若干 **阅读日**（每天 2–4 本主题相近，而非按 6 大族平铺 5 本）。
3. 每天给四样：书单 · 导读建议 · 拓展活动（详细）· **生图建议**（这张聚类是否适合做成合并复盘图，让你判断是否去生图）。
**输出**：该级别的阅读规划文档（家长据此判断哪些天值得生图）。
**适用**：探索一个新级别、想快速知道"该按什么顺序读、每天读啥、怎么拓展"。

## 模式 2 · 指定合并（跨级）

**输入**：用户指定的书单（可跨级别）+ 想要的拓展方向
**我做**：
1. 确认合并维度（同一属性、多个实例）。
2. 产出「合并复盘图内容大纲（中文）+ 画面内容指令（ImageGen）+ 拓展活动」。
**输出**：一张对比/复盘图的大纲+画面指令。
**适用**：用户已有明确想合并的书（如某周主题、某系列），要一张爆款式总结图。
**注意**：可跨级别（如 F08 Seashells + G2 某本）。

---

## 两种模式的衔接

- 模式 1 跑完后，用户对某几天说"这几本合并生图" → 切到模式 2 产出该簇的合并复盘图。
- 模式 1 的"生图建议"列就是模式 2 的候选入口。
